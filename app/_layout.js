// Importando o componente Stack da biblioteca "expo-router"
import { Stack } from "expo-router";

// Definindo o componente funcional Layout
const Layout = () => {
    // Retornando um componente Stack vazio
    return <Stack />;
}

// Exportando o componente Layout como padrão
export default Layout;
